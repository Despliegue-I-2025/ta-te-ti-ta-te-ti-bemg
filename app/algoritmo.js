function algoritmo(board, emptyPositions) {
    // Aquí va la lógica del algoritmo para decidir el movimiento
    // Por ahora, solo imprime el tablero y las posiciones vacías
    console.log('Tablero actual:', board);
    console.log('Posiciones vacías:', emptyPositions);
}
export default algoritmo;